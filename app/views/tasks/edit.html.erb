<div class="container">
  <h1>Editing Task</h1>

  <%= render 'form' %>

  <%= link_to 'Destroy', flat_task_path(@flat, @task), method: :delete, data: { confirm: 'Are you sure?' } %>
  <%= link_to 'Back', flat_tasks_path(@flat) %>
</div>
