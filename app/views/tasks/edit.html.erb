<div class="container">
  <h1>Editing Task</h1>

  <%= render 'form' %>

  <div>
    <div class="btn btn-default"><%= link_to 'Back', flat_tasks_path(@flat) %></div>

    <div class="btn btn-danger"><%= link_to 'Destroy', flat_task_path(@flat, @task), method: :delete, data: { confirm: 'Are you sure?' } %></div>
  </div>
</div>
